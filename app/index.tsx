import LoginScreen from "@/components/LoginScreen";
import { Text, View } from "react-native";

const Home = () => {
  return (
    <View>
      <LoginScreen />
    </View>
  );
};

export default Home;
